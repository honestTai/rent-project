# 公开内容与信息泄露复查

复查日期：2026-09-09。范围截止提交 `a4231d48f815a7f2090b01b5d3dc351e907ce334`；后续发行包需单独检查。

本次已检查 GitHub 公开的 3 次提交，逐个读取 797 个唯一 Git blob，其中 739 个为文本、58 个为二进制；检查对象包含源码、SQL、配置、文档及图片。GitHub 公开引用与本地历史一致，只有 `main` 分支。

已完成的复核包括：

- 凭据格式、配置字面量、高熵字符串、内网地址、联系方式及个人资料候选扫描，并核对命中位置的实际用途。
- 53 张操作截图的 OCR 与图片元数据检查，结合逐页视觉复核；品牌与背景图另行查看，没有发现个人 EXIF/GPS 元数据。
- 全部 6 次公开 Actions 运行日志和 2 份公开 Pages 产物，含成功、失败和取消的运行；共检查 267 份文本内容。
- GitHub Secret Scanning 开放告警查询：当时返回 0 条。

在上述范围内，**未发现已确认的真实部署凭据、私钥、可复用令牌或真实客户资料泄露**。命中的号码、标识和凭据示例位于隔离测试或明确的演示数据中；Actions 中的固定密码用于临时 CI 数据库与测试账号，运行令牌由 GitHub 遮罩。文档公开邮箱、HRouter 链接与 geo-console 项目链接均由维护者明确授权公开。

标准 Gitleaks 扫描仍在补充进行，结果完成后更新本页；当前不把工具尚未完成的检查计入结论。复查记录保留文件位置和类别，不在公开报告中展示疑似敏感值。

Docker 发行流程另设 `tools/check_release.py` 上传前检查：拒绝运行 `.env`、私钥文件、备份、日志、旧客户展示素材及不安全归档项，核对 manifest 的全部文件哈希，并扫描应用 JAR 的配置和类常量。第三方依赖内的样例与测试材料不被自动认定为本项目部署密钥；新发行包仍需人工复核。

本页是公开内容的信息泄露复查记录，**不是应用安全审计，也不是“没有漏洞”或“绝无泄露”的保证**。自动规则与 OCR 均有识别边界；真实支付、代扣、权限、依赖漏洞和部署暴露面需要另行评估。发现问题请按[安全反馈说明](../SECURITY.md)私下报告，不要在公开 Issue 中粘贴凭据或客户资料。
